import { SeachpippePipe } from './seachpippe.pipe';

describe('SeachpippePipe', () => {
  it('create an instance', () => {
    const pipe = new SeachpippePipe();
    expect(pipe).toBeTruthy();
  });
});
